import {
  app,
  css,
  components,
  hooks,
  modules,
  plugins,
  runtimeConfig,
  ssr,
  typescript,
} from './config';

export default defineNuxtConfig({
  app,
  css,
  components,
  hooks,
  modules,
  plugins,
  runtimeConfig,
  ssr,
  typescript,
});
